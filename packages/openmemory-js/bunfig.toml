[test]
preload = ["./test/setup.ts"]
# Sequential execution to prevent test isolation issues from shared process.env and module state
# Tests rely on singleton DB connections and environment variables
concurrency = 1

# Property-based testing configuration
# Include property tests in test discovery
# Property tests are located in test/property/ directory
