# OpenMemory Dashboard Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080
# Set this if your backend has OM_API_KEY configured for authentication
NEXT_PUBLIC_API_KEY=your
# Optional: admin key (PLAIN) used by the dashboard server to call admin-only backend endpoints.
# This should NOT be exposed to the browser. Use this only in server-side Next config.
# Example: OM_ADMIN_API_KEY_PLAIN=super-secret-admin