name: openmemory
services:
  - name: api
    environment_slug: node-js
    source_dir: backend
    build_command: bun install && bun run build
    run_command: bun run start
    http_port: 8080
    envs:
      - key: NODE_ENV
        value: production
      - key: OM_PORT
        value: 8080
