services:
  - type: web
    name: openmemory
    runtime: node
    rootDir: backend
    buildCommand: bun install && bun run build
    startCommand: bun run start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: OM_PORT
        value: 8080
