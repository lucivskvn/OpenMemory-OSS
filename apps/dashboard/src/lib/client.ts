/**
 * Re-export MemoryClient from the core package.
 * This ensures the dashboard uses the same client logic as the SDK.
 */
export { MemoryClient } from "openmemory-js/client";
export type { MemoryClientConfig } from "openmemory-js/client";
