"use client";

import { DynamicsGraph } from "@/components/DynamicsGraph";

export default function DynamicsPage() {
    return (
        <main className="p-8">
            <DynamicsGraph />
        </main>
    );
}
